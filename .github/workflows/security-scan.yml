name: Complete Security Scan

on:
  push:
    branches: ["main", "master"]
  pull_request:
    branches: ["main", "master"]

permissions:
  contents: read
  security-events: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install security tools
        run: |
          pip install bandit[sarif] pip-audit safety

      - name: Run bandit SAST scan
        run: |
          bandit -r . --exclude .git,venv,.venv,/.git,dog_breeds.db -f sarif -o bandit-results.sarif || true

      - name: Run pip-audit dependency scan
        run: |
          pip-audit -r requirements.txt --format json --output pip-audit-results.json || true

      - name: Upload SARIF results
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: bandit-results.sarif
        if: always()

      - name: Upload audit results
        uses: actions/upload-artifact@v4
        with:
          name: security-audit
          path: |
            bandit-results.sarif
            pip-audit-results.json
